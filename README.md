# Apex
This Project Is Mainly Focus on the Apex trigger LWC Concepts

…or create a new repository on the command line
echo "# angularpro" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M master
git remote add origin git@github.com:yogeshpok/angularpro.git
git push -u origin master


…or push an existing repository from the command line
git remote add origin git@github.com:yogeshpok/angularpro.git
git branch -M master
git push -u origin master